<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#1a1a2e">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Diagnostic Codes"
        android:textSize="22sp"
        android:textColor="#00d2ff"
        android:textStyle="bold"
        android:layout_marginBottom="24dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <Button
            android:id="@+id/scan_dtc_button"
            android:layout_width="0dp"
            android:layout_height="56dp"
            android:layout_weight="1"
            android:layout_marginEnd="8dp"
            android:text="Scan Codes"
            android:textAllCaps="false"
            android:backgroundTint="#0f3460"
            android:textColor="#00d2ff" />

        <Button
            android:id="@+id/clear_dtc_button"
            android:layout_width="0dp"
            android:layout_height="56dp"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="Clear Codes"
            android:textAllCaps="false"
            android:backgroundTint="#5c1010"
            android:textColor="#ff4444" />
    </LinearLayout>

    <TextView
        android:id="@+id/dtc_result_text"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="16dp"
        android:background="#16213e"
        android:padding="16dp"
        android:text="Press 'Scan Codes' to check for faults"
        android:textColor="#ccc"
        android:textSize="14sp"
        android:typeface="monospace"
        android:visibility="visible" />

</LinearLayout>
